//Crie uma função chamada `ePar` que receba um número como
//argumento e retorne `true` se o número for par e
//`false` caso contrário.

function ePar(numero) {
    return numero % 2 === 0;
}

// Exemplos de uso
console.log(ePar(1));

